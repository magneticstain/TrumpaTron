sudo: false
language: python
python:
    - "3.3"
    - "3.4"
    - "3.5"
    - "3.6"
before_script:
    - pip install -r requirements.txt
    - pip install coveralls
install: pip install .
script: build/build.sh
after_success: coveralls